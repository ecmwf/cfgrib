[tool.black]
line-length = 99

[tool.isort]
profile = "black"
line_length = 99

[tool.pytest.ini_options]
norecursedirs = [
    "build",
    "docs",
    ".tox",
]

[tool.coverage.run]
branch = true
omit = ["setup.py"]
