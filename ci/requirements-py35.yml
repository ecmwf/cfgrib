name: test_env
channels:
  - conda-forge
dependencies:
  - cffi
  - click
  - dask[array]
  - eccodes
  - nomkl
  - numpy
  - pytest
  - python=3.5
  - scipy
  - typing
  - xarray>=0.11.0
  - pip:
    - attrs
    - coveralls
    - future
    - pytest-cov
    - pytest-flakes
