name: test_env
channels:
  - conda-forge
dependencies:
  - python=3.5
  - nomkl
  - pytest
  - flake8
  - xarray
  - eccodes
  - cffi
  - pip:
    - coveralls
    - pytest-cov
    - future
    - attrs
