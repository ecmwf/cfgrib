name: test_env
channels:
  - conda-forge
dependencies:
  - nomkl
  - python=3.5
  - pytest
  - flake8
  - xarray
  - eccodes
  - cffi
  - pip:
    - coveralls
    - pytest-cov
    - future
    - attrs
