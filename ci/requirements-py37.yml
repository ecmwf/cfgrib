name: test_env
channels:
  - conda-forge
dependencies:
  - python=3.7
  - nomkl
  - pytest
  - xarray
  - eccodes
  - cffi
  - typing
  - pip:
    - coveralls
    - pytest-cov
    - pytest-flakes
    - future
    - attrs
