name: test_env
channels:
  - conda-forge
dependencies:
  - python=3.7
  - nomkl
  - pytest
  - flake8
  - xarray
  - eccodes
  - cffi
  - pip:
    - coveralls
    - pytest-cov
    - future
    - attrs
